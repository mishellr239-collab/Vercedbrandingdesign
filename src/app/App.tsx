import { AnimatedPresentation } from "./components/AnimatedPresentation";

export default function App() {
  return (
    <div className="size-full flex items-center justify-center bg-gradient-to-br from-teal-900 via-teal-700 to-cyan-600 overflow-hidden">
      <AnimatedPresentation />
    </div>
  );
}